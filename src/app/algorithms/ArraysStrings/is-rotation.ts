// Assume you have a method isSubstring that checks if one word is a substring of another.
// Given two strings s1 and s2, write code to check if s2 is a rotation of s2 using only one call to isSubstring (“waterbottle” is a rotation of “erbottlewat”)

export class IsRotation {
}
