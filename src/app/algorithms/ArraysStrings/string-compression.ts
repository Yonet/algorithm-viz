// Implement a function to perform basic string compression using the counts of repeated characters (aabcccccaaa → a2b1c5a3). If the compressed string is not smalled than original then return the original.

export class StringCompression {
}
